# Extension lisent tab with gpt and crear socket with python server

## install extension like a folder extension in chromium or similar

## in extensions tab open console and execute init() with tab gpt open.